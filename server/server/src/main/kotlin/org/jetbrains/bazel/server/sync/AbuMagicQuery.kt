package org.jetbrains.bazel.server.sync

import org.jetbrains.bazel.bazelrunner.BazelRunner

object AbuMagicQuery {
  suspend fun doMagic(bazelRunner: BazelRunner) {
    // server-side stuff to do
  }
}
